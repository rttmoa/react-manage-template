


  umi3 + dva + store + mockjs + qs + antd5
 
  搭建 Mock 环境，Mock语法 |  来自mockjs包
  CLI 命令构建 | 来自.umirc.js配置
  umi 打包配置
  Auto 目录下页面样式
  dist 目录下umi打包后的所有文件

目录
  /mock -> Mockjs 创建Mock数据

  /src/components/

  /src/models/
    @/app.js
      + 总Modal： export default { namespace: 'app' }
      + 全局 Store
      todo 路由权限处理：admin / user / develop 等

  /src/pages/ 
    /page/
      + @connect(({ app, dashboard, loading }) => ({ dashboard, loading }))
      + class Dashboard extends PureComponent {}
      +  get modalProps() {}
    /login -> 登陆页：
      + 切换国际化语言
        + dvajs 使用
    /editor -> 富文本编辑器
      + 封装文本域编辑器 （react-draft-wysiwyg）
      + 编辑器值转换为 HTML （draftjs-to-html）
      + 编辑器值转换为 MarkDown （draftjs-to-markdown）
      + 编辑器值转换为 JSON （JSON.stringify）
    /chart -> 图表
      + ECharts （3个）
      + hightCharts （21个）
      + Recharts （3个）
    /request -> 测试 request模块
      功能：
        + 左侧渲染 api 中请求列表
        + 右侧 渲染 api 的请求列表
        + 添加参数的按钮：添加 / 不添加
        + 发送按钮：发情请求
        + 结果在页面中显示
    /post -> 文章发布管理
      + Switch Tab；路由地址变化，dvajs中监听路由变化，并做相应的处理  history.push({pathname, search: qs.stringify({status: key})})
      + dvajs；post继承app： export default modelExtend(pageModel, {namespace: 'post'}
    /dashboard -> 面板
      + dvajs模块 监听路由
      + dvajs中处理请求处理发送给 /utils/modal/reducers 处理
    todo /user -> 用户的 增删改查
      + 查询用户列表
      + 创建用户
      + 删除用户
      + 修改用户
      + 查看用户详情
      + i18n 表头可中文、英文、葡萄牙文
      + 组件封装； <Filter {...this.filterProps} />  /  <List {...this.listProps} />  /  <Modal {...this.modalProps} />

      
  /src/layouts/
    @/BaseLayout.js
    @/index.js
    @/PrimaryLayout.js
    @/PublicLayout.js

  /src/locales/
    @/en 
    @/pt-br 
    @/zh


  /src/services/
    @/api.js/
      + export default { ... 请求路径 }
    @/index.js
      + const APIFunction = {}   封装APIFunction处理
  /src/themes/

  TODO 工具类
  /src/utils/
    @/